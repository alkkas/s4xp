import { Components } from '@mui/material'

const Container: Components['MuiContainer'] = {
  styleOverrides: {
    root: {
      maxWidth: 1200,
      padding: '0 15px',
    },
  },
}

export default Container
